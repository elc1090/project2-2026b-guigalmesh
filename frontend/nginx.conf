server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Necessário para SPAs com client-side routing (React Router etc.)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
